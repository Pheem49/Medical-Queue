# Medical Queue System (ระบบจองคิวออนไลน์)

ระบบจองคิวออนไลน์สำหรับคลินิกหรือโรงพยาบาล เพื่ออำนวยความสะดวกให้กับผู้รับบริการในการจองคิวล่วงหน้า และติดตามสถานะคิวได้แบบ Real-time ลดความแออัดในสถานพยาบาล

## 🌟 คุณสมบัติ (Features)

- **สำหรับผู้รับบริการ (Patients):**
  - ลงทะเบียนและเข้าสู่ระบบ (เบอร์โทรศัพท์/เลขบัตรประชาชน)
  - จองคิวตรวจล่วงหน้า เลือกแผนกและเวลาที่ต้องการ
  - ตรวจสอบสถานะคิวปัจจุบันแบบ Real-time
  - ดูประวัติการจองย้อนหลัง
  - รับการแจ้งเตือนเมื่อใกล้ถึงคิว

- **สำหรับเจ้าหน้าที่ (Staff/Admin):**
  - จัดการรายการจองคิว (เรียกคิว, ข้ามคิว, เสร็จสิ้น)
  - ดูรายชื่อผู้ป่วยในแต่ละวัน
  - จัดการตารางแพทย์และสล็อตเวลา

## 🛠️ เทคโนโลยีที่ใช้ (Tech Stack)

- **Backend:** Python (Flask), SQLite
- **Frontend:** HTML5, JavaScript (Vanilla), TailwindCSS (CDN)
- **Database:** SQLite (ไฟล์ `bookings.db`)

## 🚀 วิธีการติดตั้งและรันโปรเจกต์ (Installation & Setup)

### 1. เตรียม Backend (Python Flask)

ตรวจสอบว่าในเครื่องมี Python 3.x ติดตั้งอยู่

1. **เปิด Terminal** และเข้าไปที่โฟลเดอร์ `backend`:
   ```bash
   cd backend
   ```

2. **สร้าง Virtual Environment** (แนะนำ):
   ```bash
   # สำหรับ macOS/Linux
   python3 -m venv venv
   source venv/bin/activate

   # สำหรับ Windows
   python -m venv venv
   venv\Scripts\activate
   ```

3. **ติดตั้ง Dependencies**:
   ```bash
   pip install -r requirements.txt
   ```

4. **รัน Server**:
   ```bash
   python app.py
   ```
   Server จะทำงานที่ `http://127.0.0.1:5000`

### 2. รัน Frontend

เนื่องจาก Frontend เป็น Static Files (HTML/JS) สามารถเปิดไฟล์ได้โดยตรง หรือใช้ Live Server แนะนำให้ใช้ **Live Server** เพื่อการทำงานที่สมบูรณ์ที่สุด

1. เปิดโฟลเดอร์ `Page`
2. คลิกขวาที่ไฟล์ `index.html` แล้วเลือก **Open with Live Server** (ถ้าใช้ VS Code)
   - หรือเปิดไฟล์ `Page/index.html` ด้วย Web Browser โดยตรง (อาจมีข้อจำกัดเรื่อง CORS ในบางกรณี)

## 📖 วิธีใช้งาน (Usage)

### เข้าสู่ระบบผู้ใช้งานทั่วไป (Patient)
1. เปิดหน้าเว็บ `index.html`
2. เลือกแท็บ **"ผู้รับบริการ (คนไข้)"**
3. หากยังไม่มีบัญชี ให้กด **"ลงทะเบียนคนไข้ใหม่"**
4. กรอกเบอร์โทร หรือ เลขบัตรประชาชน และรหัสผ่านเพื่อเข้าสู่ระบบ
5. เลือกเมนู **"จองคิวตรวจ"** เพื่อทำการนัดหมาย

### เข้าสู่ระบบเจ้าหน้าที่ (Staff)
1. เปิดหน้าเว็บ `index.html`
2. เลือกแท็บ **"เจ้าหน้าที่"**
3. กรอก Username และ Password (เช่น `admin` / `admin` หากมีการตั้งค่าไว้ในฐานข้อมูล)
4. จะเข้าสู่หน้า Dashboard สำหรับบริหารจัดการคิว

## 📂 โครงสร้างโปรเจกต์ (Project Structure)

```
Medical-Queue/
├── backend/            # ส่วนของ Server และ Database
│   ├── app.py          # ไฟล์หลักสำหรับรัน Server
│   ├── database.py     # จัดการการเชื่อมต่อฐานข้อมูล
│   ├── requirements.txt # รายชื่อ Libraries ที่ต้องใช้
│   ├── routes/         # API Endpoints แยกตามหมวดหมู่
│   └── ...
├── Page/               # หน้าเว็บ Frontend (HTML/JS)
│   ├── index.html      # หน้า Login
│   ├── home.html       # หน้าหลัก
│   ├── booking.html    # หน้าจองคิว
│   └── ...
└── docs/               # เอกสารประกอบโปรเจกต์
```

---
**หมายเหตุ:** ตรวจสอบให้แน่ใจว่า Backend รันอยู่ตลอดเวลาการใช้งานหน้าเว็บ เพื่อให้การดึงข้อมูลทำงานได้ถูกต้อง
